<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:showIn="@layout/activity_main"
        android:paddingTop="150dp"
        tools:context="com.example.david_lewis.martaticketing.MainActivity" android:background="#d4a655">

    <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Username"
            android:inputType="text"
            android:id="@+id/editTxtUsername"
            android:layout_marginEnd="35dp"
            android:layout_marginStart="35dp"
            android:layout_marginBottom="10dp"

    />
    <EditText android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:hint="Password"
              android:inputType="textPassword"
              android:layout_below="@id/editTxtUsername"
              android:layout_marginEnd="35dp"
              android:layout_marginStart="35dp"
              android:layout_marginBottom="12dp"
              android:id="@+id/editTxtPassword"
    />

    <Button
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:text="Log in"
            android:id="@+id/btnLogIn"
            android:layout_below="@id/editTxtPassword"
            android:layout_marginStart="35dp"
           />
    <Button
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:text="Register"
            android:layout_toEndOf="@id/btnLogIn"
            android:layout_below="@id/editTxtPassword"
            android:layout_marginStart="35dp"
            android:id="@+id/btnRegister"/>

    <ImageView android:layout_width="wrap_content"
               android:contentDescription="Logo for train app"
               android:layout_height="wrap_content"
               android:layout_alignParentBottom="true"
               android:layout_alignParentEnd="true"
               android:layout_margin="15dp"
               android:src="@drawable/marta_logo"
    />
    <ImageView android:layout_width="wrap_content"
               android:contentDescription="Logo for train app"
               android:layout_height="wrap_content"
               android:layout_alignParentBottom="true"
               android:layout_alignParentStart="true"
               android:layout_margin="10dp"
               android:src="@drawable/train_logo"
    />

</RelativeLayout>
